---
sidebar_label: Logging
---

# Configure logging for Bigbytes

By default Bigbytes logs to the console in a human readable format. You can configure the logging behaviour, such
as logging to a file or using a JSON format, by
using the following environment variables:

| Variable                       | Description                                                 | Required? | Default                |
| ------------------------------ | ----------------------------------------------------------- | --------- | ---------------------- |
| `BIGBYTES_LOG_LEVEL`          | The minimum level of log messages to display                |           | `INFO`                 |
| `BIGBYTES_LOG_FORMAT`         | The format of log messages                                  |           | `pretty`               |
| `BIGBYTES_LOG_OUTPUTS`        | The outputs to send log messages to                         |           | `console`              |
| `BIGBYTES_LOG_CONSOLE_LEVEL`  | The minimum level of log messages to display on the console |           | `BIGBYTES_LOG_LEVEL`  |
| `BIGBYTES_LOG_CONSOLE_FORMAT` | The format of log messages on the console                   |           | `BIGBYTES_LOG_FORMAT` |
| `BIGBYTES_LOG_FILE_LEVEL`     | The minimum level of log messages to write to the log file  |           | `BIGBYTES_LOG_LEVEL`  |
| `BIGBYTES_LOG_FILE_FORMAT`    | The format of log messages in the log file                  |           | `BIGBYTES_LOG_FORMAT` |
| `BIGBYTES_LOG_FILE_PATH`      | The path to the log file                                    |           | `./logs/all.log`       |
